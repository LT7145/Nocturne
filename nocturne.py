import argparse
import sys

def main():
    parse = argparse.ArgumentParser(
            prog="nocturne"
            description="Nocturne - Automated Recon Pipeline"
            )
    parser.add_argument
